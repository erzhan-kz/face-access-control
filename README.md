
# Face Access Control - Cloud package

Frontend: React (Vite)
Backend: Node.js (Express) + SQLite

Deploy:
1. Push this repository to GitHub.
2. Create a Render.com account and connect to GitHub.
3. Create a new web service from this repo (branch main).
Render will run the buildCommand and start backend. Frontend is built and proxied to /api.
